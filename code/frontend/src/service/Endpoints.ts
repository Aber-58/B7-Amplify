export enum Endpoints {
    LOGIN='login',
    ADMIN='admin',
    VALIDATE='validate',
    JOIN='join',
    POLL='poll',
    TOPIC='topic',
    GET_CIRCLE_SIZES='get_circle_sizes',
    CLUSTERS='clusters',
    CLUSTER='trigger_clustering'
}